{
  "name": "circuit",
  "private": true,
  "version": "1.0.0",
  "description": "The Circuit. DFW restaurant field book as a Cloudflare Worker PWA.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
